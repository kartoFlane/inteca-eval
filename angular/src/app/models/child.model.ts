export class Child {
  constructor(
    public id: number,
    public firstName: string,
    public secondName: string,
    public birthDate: string,
    public pesel: string,
    public sex: string
  ) {
  }
}
